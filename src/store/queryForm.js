// import { observable, action } from 'mobx'

class QueryForm {
  query = {}
  // @observable.shallow query = {}

  // @action
  // setQuery = query => {
  //   this.query = query
  // }
}

export default new QueryForm()
