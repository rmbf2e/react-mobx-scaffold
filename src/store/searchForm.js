// import { observable, action } from 'mobx'

class SearchForm {
  query = {}
  // @observable.shallow query = {}

  // @action
  // setQuery = query => {
  //   this.query = query
  // }
}

export default new SearchForm()
